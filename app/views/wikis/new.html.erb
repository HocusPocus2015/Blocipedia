<h1>New Wiki</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for Wikis</p>
      <ul>
        <li>Create an interesting title that GRABS attention.</li>
        <li>Do not use offensive language.</li>
        <li>The incessant use of hashtags will get you banned.</li>
      </ul>
  </div>

  <div class="col-md-8">
    <%= form_for @wiki do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control', placeholder: "Enter wiki title" %>
      </div>
      <div class="form-group">
        <%= f.label :body %>
        <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter wiki content" %>
      </div>

<!--  #does not work, but would be better to call on model -->
       <!-- <div class="form-group">
           <select id="topic" class="form-control">
             <option><%#= Topic.self %></option>
           </select>
         </div>
       </div> -->

<!-- # works, but is no better than href version -->
       <div class="form-group">
           <select id="topic" class="form-control">
             <option>Culture and the arts</option>
             <option>Geography and places</option>
             <option>Health and fitness</option>
             <option>History and events</option>
           </select>
         </div>
       </div>

<!-- # does not work either -->
       <!-- <div class="form-group">
        <%#= f.select(:topic) %>
      </div> -->

       <div class="form-group">
         <%= f.submit "Save", class: 'btn btn-success' %>
       </div>
     <% end %>
   </div>
 </div>
